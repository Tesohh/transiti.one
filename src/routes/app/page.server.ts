// // import { spotify } from "$lib/spotify"
// import type { PageServerLoad } from "./$types.js"

// export async function load() {
//   const track = (await spotify.tracks.search("shqiptari"))[0]
//   return { text: track.name }
// }
